# tg-export-txt-mcp

Read-only MCP server for Telegram TXT exports generated by `tg_backup`.

It exposes:

- `read_export_file`: read one transcript file under the configured export root
- `search_exports`: search transcript files with `rg`

The service is exposed over HTTP at `/mcp`.
