# filesystem-mcp

Filesystem MCP server, exposed over HTTP at `/mcp`.

It ports the tool surface of `/Users/cofob/Development/mcp-filesystem-server`
to Python and restricts all reads and writes to one configured root directory.
